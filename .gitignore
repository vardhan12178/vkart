# Node modules
node_modules/
**/node_modules/

# Build / dist
build/
dist/
**/build/
**/dist/

# Env files (keep an example instead)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
!.env.example

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Yarn PnP (if ever used)
.pnp.cjs
.pnp.loader.mjs
.pnp.js
.pnp/

# Coverage
coverage/
**/coverage/

# (Optional) don’t ignore lockfiles — recommend committing them
# package-lock.json
# yarn.lock

# If you really need Netlify redirects at repo root:
# (no special rule needed if you don’t ignore the root)
# _redirects
